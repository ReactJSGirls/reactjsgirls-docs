---
name: Set Up
menu: Tinder For Dogs
route: /tinder-for-dogs/set-up
---


import Code from '../components/code'

# Set Up

| Project Goal           |Make an awesome React App                                                                                                                           |
| ------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| What you’ll learn | Create a React App that will get data from an API and change it locally                                                                                     |
| Tools you’ll need      | A modern browser like Chrome. Access to [Codesandbox](https://codesandbox.io) |
| Time needed to complete     | 1 hour                                                                                                                                                                        |
| Just want to try the app?   | [Code Sandbox link](https://codesandbox.io/s/github/SaraVieira/workshop-one-components)                                                                                                                      |

<Code code={`
<pre className={className} style={style}>
  {tokens.map((line, i) => (
    <div {...getLineProps({ line, key: i })}>
      {line.map((token, key) => (
        <span {...getTokenProps({ token, key })} />
      ))}
    </div>
  ))}
</pre>
`}/>

Hello, I'm a mdx file!
